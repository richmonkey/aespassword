{
  "name": "aespassword",
  "version": "0.1.0",
  "main": "main.js",
  "description": "aespassword",
  "productName": "aespassword",
  "scripts": {
    "dev": "electron ./app",
    "clean": "rm -rf ./dist",
    "clean:osx": "rm -rf ./dist/osx",
    "clean:win": "rm -rf ./dist/win",
    "build": "npm run clean && npm run build:osx && npm run build:win",
    "build:osx": "npm run clean:osx && electron-packager ./app --platform=darwin --arch=x64 --version=0.30.2 --icon=assets/osx/panda.icns --out=dist/osx --sign=\"iPhone Developer: Zhifeng Zhou\" --overwrite aespassword",
    "build:win": "npm run clean:win && electron-packager ./app --icon=assets/win/panda.ico --out=dist/win --platform=win32 --arch=ia32 --version=0.30.2 --overwrite aespassword"
  },
  "devDependencies": {
    "grunt": "^0.4.5",
    "grunt-appdmg": "^0.3.2",
    "grunt-electron-installer": "^1.0.2"
  }
}
